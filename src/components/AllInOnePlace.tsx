import { Heading } from '@mycrypto/ui';
import Section from './Section';

export const AllInOnePlace = () => {
  return (
    <Section type="landing" alignItems="center" relative>
      <Heading>All your ETH. All in one place.</Heading>
    </Section>
  );
};
